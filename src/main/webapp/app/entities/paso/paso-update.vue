<template>
    <div class="row justify-content-center">
        <div class="col-8">
            <form name="editForm" role="form" novalidate v-on:submit.prevent="save()" >
                <h2 id="kbaseApp.paso.home.createOrEditLabel" v-text="$t('kbaseApp.paso.home.createOrEditLabel')">Create or edit a Paso</h2>
                <div>
                    <div class="form-group" v-if="paso.id">
                        <label for="id" v-text="$t('global.field.id')">ID</label>
                        <input type="text" class="form-control" id="id" name="id"
                               v-model="paso.id" readonly />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('kbaseApp.paso.paso')" for="paso-paso">Paso</label>
                        <input type="number" class="form-control" name="paso" id="paso-paso"
                            :class="{'valid': !$v.paso.paso.$invalid, 'invalid': $v.paso.paso.$invalid }" v-model.number="$v.paso.paso.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('kbaseApp.paso.desc')" for="paso-desc">Desc</label>
                        <input type="text" class="form-control" name="desc" id="paso-desc"
                            :class="{'valid': !$v.paso.desc.$invalid, 'invalid': $v.paso.desc.$invalid }" v-model="$v.paso.desc.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-bind:value="$t('kbaseApp.paso.instruccion')" for="paso-instruccion">Instruccion</label>
                        <select class="form-control" id="paso-instruccion" name="instruccion" v-model="paso.instruccionId">
                            <option v-bind:value="null"></option>
                            <option v-bind:value="instruccionOption.id" v-for="instruccionOption in instruccions" :key="instruccionOption.id">{{instruccionOption.id}}</option>
                        </select>
                    </div>
                </div>
                <div>
                    <button type="button" id="cancel-save" class="btn btn-secondary" v-on:click="previousState()">
                        <font-awesome-icon icon="ban"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.cancel')">Cancel</span>
                    </button>
                    <button type="submit" id="save-entity" :disabled="$v.paso.$invalid || isSaving" class="btn btn-primary">
                        <font-awesome-icon icon="save"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.save')">Save</span>
                    </button>
                </div>
            </form>
        </div>
    </div>
</template>
<script lang="ts" src="./paso-update.component.ts">
</script>
